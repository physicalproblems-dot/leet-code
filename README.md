# leet-code
I am going to solve all the important problems of leet-code
# Git command
git add duplicate_zeros.py 
git commit -m "duplicate zeroes"
git push origin main  

#! To calibrate changes in github and local machinde database
git pull --rebase origin main
git push origin main
